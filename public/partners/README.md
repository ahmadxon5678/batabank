Partner image fallbacks.

Replace these files with the final approved assets if needed:

- `orikguli.svg` -> the provided O'rikguli photo
- `goethe-institut.svg` -> the provided Goethe-Institut logo

Keep the filenames unchanged so the landing page keeps working.
